node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Логи сборки и отладки
*.log

# Данные панели: срез генерирует `skidki export` (в CI — перед публикацией).
# В репозитории держим только placeholder, чтобы панель собиралась и пустой.
static/data/history.json
